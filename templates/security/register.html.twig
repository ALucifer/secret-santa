{% extends 'security/index.html.twig' %}

{% block content %}
    <div class="flex flex-col">
        <h2 class="text-center">Inscription</h2>
        {{ form(form, { 'attr': { 'class': 'w-full' }}) }}
    </div>
{% endblock %}

{% block information %}
<p class="text-center">Si vous avez déjà un compte, connectez-vous <a href="{{ url('app_login') }}">ici</a></p>
{% endblock %}